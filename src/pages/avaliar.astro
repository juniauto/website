---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';

const title = "Avalie sua Experiência | Juniauto";
const description = "Sua opinião é fundamental para mantermos a excelência da Juniauto. Conte-nos como foi seu atendimento.";
const googleReviewLink = "https://search.google.com/local/writereview?placeid=ChIJexi7VdbjmwARMqBMzfmbsog";
---

<Layout title={title} description={description}>
  <Navigation />
  
  <main class="pt-32 pb-20 min-h-screen flex items-center justify-center bg-[#0a0a0a]">
    <div class="container mx-auto px-4 max-w-2xl">
      <div class="glass-card p-8 md:p-12 text-center border-[#ffd600]/10">
        <!-- Step 1: Star Rating -->
        <div id="step-1">
          <h1 class="text-white text-4xl md:text-5xl font-black mb-6 uppercase tracking-tighter italic">
            Como foi seu <span class="text-[#ffd600]">atendimento?</span>
          </h1>
          <p class="text-gray-400 text-lg mb-10">
            Sua avaliação ajuda a Juniauto a manter o padrão de excelência em Campo Grande.
          </p>
          
          <div class="flex justify-center gap-2 md:gap-4 mb-10" id="star-rating">
            {[1, 2, 3, 4, 5].map((star) => (
              <button 
                data-rating={star}
                id={`star-${star}`}
                aria-label={`Avaliar ${star} estrela${star > 1 ? 's' : ''}`}
                class="star-btn text-5xl md:text-6xl text-gray-800 hover:text-[#ffd600] transition-all transform hover:scale-110 focus:outline-none"
              >
                ★
              </button>
            ))}
          </div>
          <p class="text-gray-600 text-sm uppercase font-bold tracking-widest">Toque para avaliar</p>
        </div>

        <!-- Step 2: Negative (1-4 stars) -->
        <div id="step-negative" class="hidden animate-fade-in">
          <h2 class="text-white text-3xl font-bold mb-6 italic uppercase">Sentimos muito!</h2>
          <p class="text-gray-400 mb-8 leading-relaxed">
            Nossa missão é o resgate perfeito. O que poderíamos ter feito melhor hoje? Seu feedback será analisado diretamente por nossa diretoria.
          </p>
          <textarea 
            id="negative-feedback-text"
            class="w-full bg-white/5 border border-white/10 rounded-xl p-6 text-white placeholder:text-gray-600 focus:border-[#ffd600] outline-none transition-colors mb-6 text-lg"
            rows="4"
            placeholder="Ex: Demora no atendimento, comunicação..."
          ></textarea>
          <button 
            id="submit-negative"
            class="w-full bg-[#ffd600] text-black font-black py-5 rounded-xl uppercase tracking-wider hover:brightness-110 active:scale-95 transition-all text-lg shadow-xl"
          >
            Enviar Feedback Interno
          </button>
          
          <button 
            data-back
            class="mt-8 text-gray-500 hover:text-white transition-colors text-xs uppercase font-bold tracking-widest"
          >
            ← Voltar
          </button>
        </div>

        <!-- Step 3: Positive (5 stars) -->
        <div id="step-positive" class="hidden animate-fade-in">
          <div class="mb-8 inline-flex items-center justify-center w-24 h-24 rounded-full bg-[#ffd600]/10 text-[#ffd600] text-5xl border border-[#ffd600]/20">
            ⭐
          </div>
          <h2 class="text-white text-3xl md:text-4xl font-black mb-6 italic uppercase">Excelente!</h2>
          <p class="text-gray-400 mb-8 leading-relaxed">
            Ficamos felizes em ajudar! Para nos apoiar, conte o que mais gostou e <strong>cole no Google Maps</strong>. Isso ajuda outros motoristas em Campo Grande!
          </p>
          
          <textarea 
            id="positive-feedback-text"
            class="w-full bg-white/5 border border-white/10 rounded-xl p-6 text-white placeholder:text-gray-600 focus:border-[#ffd600] outline-none transition-colors mb-6 text-lg"
            rows="3"
            placeholder="Ex: Atendimento super rápido e profissional!"
          ></textarea>

          <div class="space-y-4">
            <button 
              id="copy-and-go"
              aria-label="Copiar texto e abrir Google Maps para avaliar"
              class="flex items-center justify-center gap-3 w-full bg-[#ffd600] text-black font-black py-5 rounded-xl uppercase tracking-wider hover:brightness-110 active:scale-95 transition-all text-xl shadow-xl"
            >
              <svg class="w-6 h-6" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                <path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                <path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                <path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
                <path d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
              </svg>
              Copiar e ir para o Google
            </button>
            <p id="copy-success" class="text-[#ffd600] text-sm font-bold hidden animate-pulse">✓ Texto copiado! Redirecionando...</p>
            
            <a 
              id="skip-to-google"
              href={googleReviewLink} 
              target="_blank"
              rel="noopener noreferrer"
              class="block text-gray-500 hover:text-[#ffd600] transition-colors text-sm font-bold uppercase tracking-widest pt-4"
            >
              Pular e apenas avaliar
            </a>
          </div>

          <button 
            data-back
            class="mt-12 text-gray-600 hover:text-white transition-colors text-xs uppercase font-bold tracking-widest block mx-auto"
          >
            ← Escolher outra nota
          </button>
        </div>

        <!-- Success Screen -->
        <div id="step-thanks" class="hidden animate-fade-in">
          <div class="mb-8 inline-flex items-center justify-center w-24 h-24 rounded-full bg-green-500/10 text-green-500 text-5xl border border-green-500/20">
            ✓
          </div>
          <h2 class="text-white text-3xl font-black mb-4 uppercase italic">Obrigado!</h2>
          <p class="text-gray-400 mb-10 text-lg leading-relaxed">
            Sua opinião é fundamental. Continuaremos trabalhando para ser o melhor socorro 24h da região.
          </p>
          <a href="/" class="inline-block bg-white/5 border border-white/10 text-white px-10 py-4 rounded-xl font-bold uppercase tracking-widest hover:bg-white/10 transition-all">
            Voltar para Home
          </a>
        </div>
      </div>
    </div>
  </main>

  <Footer />
</Layout>

<script is:inline define:vars={{ googleReviewLink }}>
  const stars = document.querySelectorAll('.star-btn');
  const step1 = document.getElementById('step-1');
  const stepNegative = document.getElementById('step-negative');
  const stepPositive = document.getElementById('step-positive');
  const stepThanks = document.getElementById('step-thanks');
  const submitNegative = document.getElementById('submit-negative');
  const copyAndGoBtn = document.getElementById('copy-and-go');
  const positiveFeedbackText = document.getElementById('positive-feedback-text');
  const copySuccess = document.getElementById('copy-success');
  const backButtons = document.querySelectorAll('[data-back]');

  let currentRating = 0;

  stars.forEach(btn => {
    btn.addEventListener('mouseover', () => {
      const rating = parseInt(btn.dataset.rating);
      updateStars(rating);
    });

    btn.addEventListener('mouseout', () => {
      updateStars(currentRating);
    });

    btn.addEventListener('click', () => {
      currentRating = parseInt(btn.dataset.rating);
      if (currentRating === 5) {
        showStep(stepPositive);
      } else {
        showStep(stepNegative);
      }
      
      if (window.gtag) {
        window.gtag('event', 'rating_selected', {
          'rating': currentRating
        });
      }
    });
  });

  function updateStars(rating) {
    stars.forEach(btn => {
      const btnRating = parseInt(btn.dataset.rating);
      if (btnRating <= rating) {
        btn.classList.add('text-[#ffd600]');
        btn.classList.add('scale-110');
        btn.classList.remove('text-gray-800');
      } else {
        btn.classList.remove('text-[#ffd600]');
        btn.classList.remove('scale-110');
        btn.classList.add('text-gray-800');
      }
    });
  }

  function showStep(step) {
    [step1, stepNegative, stepPositive, stepThanks].forEach(s => s.classList.add('hidden'));
    step.classList.remove('hidden');
    window.scrollTo({ top: 0, behavior: 'smooth' });
  }

  submitNegative.addEventListener('click', () => {
    const feedback = document.getElementById('negative-feedback-text').value;
    
    if (window.gtag) {
      window.gtag('event', 'internal_feedback_submit', {
        'rating': currentRating,
        'feedback': feedback
      });
    }

    showStep(stepThanks);
  });

  copyAndGoBtn.addEventListener('click', async () => {
    const text = positiveFeedbackText.value;
    
    if (window.gtag) {
      window.gtag('event', 'google_review_click', {
        'has_text': !!text
      });
    }

    if (text) {
      try {
        await navigator.clipboard.writeText(text);
        copySuccess.classList.remove('hidden');
        setTimeout(() => {
          window.open(googleReviewLink, '_blank');
          showStep(stepThanks);
        }, 1200);
      } catch (err) {
        window.open(googleReviewLink, '_blank');
        showStep(stepThanks);
      }
    } else {
      window.open(googleReviewLink, '_blank');
      showStep(stepThanks);
    }
  });

  backButtons.forEach(btn => {
    btn.addEventListener('click', () => {
      currentRating = 0;
      updateStars(0);
      showStep(step1);
    });
  });
</script>

<style>
  .animate-fade-in {
    animation: fadeIn 0.4s ease-out forwards;
  }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  textarea {
    resize: none;
  }

  .glass-card {
    background: rgba(255, 255, 255, 0.03);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 24px;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
  }
</style>
