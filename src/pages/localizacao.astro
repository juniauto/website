---
import { getCollection } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
import WhatsappSection from '../components/WhatsappSection.astro';

const neighborhoods = await getCollection('neighborhoods');
neighborhoods.sort((a, b) => a.data.name.localeCompare(b.data.name));

const title = "Localização | Onde a Juniauto Atende em Campo Grande";
const description = "Veja nossa área de cobertura completa na Zona Oeste. Base estratégica em Campo Grande para atendimento rápido em Bangu, Realengo, Santa Cruz e mais.";
---

<Layout title={title} description={description}>
  <Navigation />
  
  <main class="pt-32 pb-20">
    <div class="container mx-auto px-4">
      <div class="text-center mb-20">
        <h1 class="text-white text-5xl md:text-7xl font-black mb-6 uppercase tracking-tighter">Área de Cobertura</h1>
        <p class="text-gray-400 text-xl max-w-3xl mx-auto">Sediados em Campo Grande com força tarefa em toda a Zona Oeste. Chegamos rápido onde você estiver.</p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 mb-20">
        <div class="lg:col-span-2">
           <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
             {neighborhoods.map((n) => (
                <a href={`/reboque-em/${n.slug}`} class="glass-card p-6 flex items-center justify-between group hover:border-[#ffd600]/50 transition-all">
                  <span class="text-white font-bold text-lg group-hover:text-[#ffd600]">{n.data.name}</span>
                  <span class="text-[#ffd600] opacity-0 group-hover:opacity-100 transition-opacity">Ver Detalhes →</span>
                </a>
             ))}
           </div>
        </div>

        <div class="lg:col-span-1">
          <div class="sticky top-32 glass-card p-10 border-[#ffd600]/20">
            <h2 class="text-[#ffd600] text-3xl font-black mb-6 uppercase italic">Base Tática</h2>
            <p class="text-gray-300 mb-8 leading-relaxed">
              Nossa frota está baseada estrategicamente em **Campo Grande**, com unidades móveis circulando pelos eixos da **Avenida Brasil**, **Cesário de Melo** e **Santa Cruz**.
            </p>
            <div class="space-y-4">
               <div class="flex items-start gap-4 p-4 rounded-xl bg-white/5 border border-white/10">
                  <span class="text-2xl">⚡</span>
                  <div>
                    <h4 class="text-white font-bold">Resposta Imediata</h4>
                    <p class="text-gray-400 text-sm">20-40 min em média.</p>
                  </div>
               </div>
               <div class="flex items-start gap-4 p-4 rounded-xl bg-white/5 border border-white/10">
                  <span class="text-2xl">🛡️</span>
                  <div>
                    <h4 class="text-white font-bold">Segurança 24h</h4>
                    <p class="text-gray-400 text-sm">Prontos para resgate noturno.</p>
                  </div>
               </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <WhatsappSection />
  </main>

  <Footer />
</Layout>

